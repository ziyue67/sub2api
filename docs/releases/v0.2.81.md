> AI API Gateway Platform - 将 AI 订阅配额分发和管理

> [!IMPORTANT]
> 本版本为 **0.2.81**：在完整保留 Fork 自有功能的前提下，将 `ranxi2001/sub2api` 的功能全量移植进本 Fork。
> `latest` 与 `0.2.81` 均指向本版本。
>
> 版本号使用三段式 `0.2.81` 而非 `0.2.8.1`：GoReleaser 强制语义化版本，四段号会被拒绝；
> 且应用内更新检查的 `parseVersion` 只取前三段，`0.2.8.1` 会被误判为 `0.2.8` 而导致收不到更新。

> [!NOTE]
> **本次为 0.2.81 的重发**（2026-09-26）：首次发布基于 ranxi `v2.8.13`，本次在此基础上
> 追平至 `v2.8.14`，并调整发布产物的范围。
>
> - **镜像只提供 `linux/amd64` 单架构**（不再产出 arm64；`install.sh` 在 arm64 上会明确报错）
> - **标签只保留 `0.2.81` 与 `latest`**（不再产出 `0.2` / `0` 这类大版本浮动标签）
> - 安装脚本与应用内更新/回滚的默认仓库已由上游 `Wei-Shaw/sub2api` 改回本 Fork

## 移植来源

- 来源仓库：`ranxi2001/sub2api` 分支 `production`
- 来源提交：`594cdf0d6`（release `v2.8.14` 及其后合并的 #85 – #92 修复）
- 上次移植基准：`3e345632f`（release `v2.8.13` 及其后的 #80 – #83 修复）
- 本次增量：14 个非合并提交，71 个文件（+2238 / −80）
- 合并基准：上游 `Wei-Shaw/sub2api` 的 `3c2f05c9`
- 版本号：`backend/cmd/server/VERSION` 为 `0.2.81`

## v2.8.14 增量（本次新增）

- **Excel / BPS 图片中转可配置容量**：最大在途请求数可在「系统设置」中配置，字节预算与 128 槽容量校验按配置联动，修复固定预算在高并发下的截断
- **BPS 403 自动关闭协议**：账号凭据与两个开关仍然匹配时，收到 403 自动只关闭该账号的 BPS，不影响其他能力
- **BPS 用量快照与 429 冷却**：补齐用量快照上报与 429 冷却窗口
- **Excel / BPS 账号工具往返探测**：新增账号测试弹窗中的工具调用往返探测（tool roundtrip probe）
- **BPS 代码工具嵌套转义修复**：修复 function code transport 的嵌套转义问题，新增独立传输实现与回归测试
- **长上下文计费徽标可配置**：使用详情中的「长上下文计费 x2」徽标支持开关配置，默认开启
- **判题修复**：只比对参考值与候选值，不再把题目下发给被测模型
- **文档**：新增[新人入门文档](../新人入门.md)与 [Excel / BPS 容量验证](../excel-bps-capacity-validation.md)

## 移植的主要功能

- **管理员请求采集**：按用户 / 上游账户 / 入口分组创建 1–1440 分钟的采集任务，对照请求、响应、工具调用与流式事件；默认关闭，仅保留确认出错的请求或 WebSocket 轮次，正常请求结束即排队清理临时正文
- **Excel / BPS 协议**：单工具调用参数解析、批量设置协议开关与模型范围、缓存创建 token 转普通输入、图片中转入驻控制，以及用量明细中的推理等级映射
- **凭证守护**：令牌巡检、自动重登与错误态自愈（默认关闭）
- **账号质量运维**：检测规则、判定与告警、批量设置 Excel / BPS 选项
- **Codex 并行收割与 780 原生铸造**、**Codex 门票（x-codex-turn-state）打票与注入**
- **Mihomo 托管 / 国家过滤 / 动态代理区域**：含 Codex 门票出口池与本地内核迁移脚本
- **Pelican 定时演练与展示页**
- **请求时延明细**：新增请求耗时与 TPS 统计口径
- **分组维度扩容**：上游分组倍率、分组允许模型、用户分组拒绝模型、仅流式分组
- **Copilot SDK 接入教程**与 **Cyber 会话屏蔽**能力
- **字节点（BPS）自定义传输层**与相关回归测试

## Fork 自有功能（完整保留）

- **Codex 292 门票（x-codex-turn-state）打票与注入**：独立代理后台打票、住宅 IP 请求覆盖该头，含后台开关、门票状态展示与缺票拦截
- **上游剩余额度探测**：账号页可查询上游余额与用量窗口
- **账号代理车道（proxy lanes）**：车道级调度、车道粘性绑定与准入并发控制
- **图片创作 / 画布 / Studio Bridge**、**邮件广播**、**模型广场（model-square）** 等
- **发布流水线**：双 registry（GHCR + Docker Hub）推送；`latest` 只由 Release 写，`0.2` / `0` 浮动标签已取消

## 升级说明

- 启动时会执行新增迁移 `239` – `251`（含 `249` 的两个迁移文件）。升级前请备份数据库。
- 请求采集正文默认写入 `DATA_DIR/request-captures`；未配置 `DATA_DIR` 时使用 `./data/request-captures`。
- 请求采集与凭证守护默认关闭，需在「系统设置 > 功能开关」中显式启用。
- 安装脚本与在线更新服务的默认仓库已修正为 `ziyue67/sub2api`（此前默认值为上游 `Wei-Shaw/sub2api`，
  会导致按本 Fork 的版本号安装时 404，以及应用内「检查更新」查到上游 Release）。

## 验证

- 合并冲突全部人工解决，逐文件核对两侧功能并保留并集；合并结果已按文件级哈希校验，确认未丢失两侧任何一侧的内容改动。
- 本地校验：后端 `go build ./...`、`go vet -tags=unit ./...` 通过；前端 `vue-tsc --noEmit`、i18n 键完整性、eslint 通过。
- 发布配置校验：6 个 YAML 解析通过，并断言 `goarch` 仅 `amd64`、无 arm64、无 `{{ .Major }}` 浮动标签、
  manifest 仅 `0.2.81` 与 `latest`。CI 的部署脚本测试本机全通过
  （`apple-container-test.sh` 依赖 macOS 的 `stat -f`，本机不适用，由 CI 的 macos-15 作业覆盖）。
- 单元测试仅剩 4 个用例失败，已在未合并的 `main` 基线上逐一复现（同一断言、同一数值），属 Windows 环境限制（无法表达 POSIX 文件权限、本地 socket 行为差异），非合并引入。
- 合并提交在 `main` 上通过 `CI` 与 `Security Scan`。
